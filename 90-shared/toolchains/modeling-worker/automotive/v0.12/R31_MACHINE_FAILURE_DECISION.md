# Modeling Worker v0.12｜E3 R3.1 Machine Failure Decision

Status: `R3.1 CONTRACT PASS / MACHINE FAIL / TERMINATION GEOMETRY + EVIDENCE-ZONING REVISE / HUMAN NOT OPEN / PAP BLOCKED / PROMOTION BLOCKED`

## Immutable lineage

- GitHub Actions workflow: `OLEANDER Modeling Worker v0.12 R3.1`
- run: `31672155972` / R3.1 run #1
- tested head: `e8e18e67a2b4a5b1edc85e0c2a75cb127953bcc3`
- Contract job: `SUCCESS`
- Blender Relationship-Aware Surface QA: `FAILURE`
- artifact: `9170303850`
- digest: `sha256:c72e9f5895974daebae0f862686478e0e87ab5760a69a0e5b848f803215f2995`

Human Project / Visual PASS was not opened because Machine QA did not clear.

## Retained architecture evidence

The following R3.1 findings remain valid and are not reopened by R3.2:

- five explicit Surface Source families: Upper / Side / Lower / Front-Termination / Rear-Termination;
- Profile and Plan-view Primary Curve authority separated;
- global single cage forbidden;
- blanket continuity class forbidden;
- source G1 relationship authority passes independently of finite-difference runtime diagnostics;
- semantic source ownership is disjoint;
- semantic edits are exact and produce direct declared-authority effects;
- profile silhouette, plan-view rear-haunch hierarchy and reflection-field proxies pass;
- execution topology remains derived from editable Surface Source Authority.

## Actual R3.1 Machine failure

The false checks were:

- `broad_interior_fairness_pass = false`;
- `character_band_quality_pass = false`;
- therefore `all_semantic_variants_surface_pass = false`.

Main Upper / Side / Lower broad interiors already satisfied the existing `0.020 m` minimum normal-radius proxy. The main shoulder / rocker character bands also satisfied the existing `0.010 m / 105°` character-band contract.

The failing minima concentrated in the Front / Rear termination families and in evidence classification around inherited shoulder / rocker bands and the declared far intentional boundaries:

- Front termination broad minimum radius: approximately `0.00987 m`;
- Rear termination broad minimum radius: approximately `0.01480 m`;
- Front far-transition total normal turn: approximately `151.73°`;
- Rear far-transition total normal turn: approximately `159.42°`.

The same report confirmed all four finite-difference boundary diagnostics below the separate `0.50°` runtime diagnostic limit, while source-level G1 authority remained PASS.

## Root cause

`R3 architecture = RETAIN`

`R3.1 termination continuation geometry + QA zoning = REVISE`

Two distinct defects are present:

1. Front / rear continuation geometry still loses too much width / vertical volume toward the far benchmark boundary.
2. The R3.1 implementation still counts inherited shoulder / rocker character bands inside termination `BROAD_INTERIOR`, and also counts far `INTENTIONAL_BOUNDARY` transitions inside the ordinary Character-Band PASS. This contradicts the evidence-class split that R3.1 itself declares.

This is not permission to relax the existing quality thresholds.

## R3.2 allowed correction

- retain the five-family R3 architecture and Profile / Plan authority split;
- retain all existing quality thresholds (`0.020 m` broad radius, `0.010 m` character radius, `105°` character turn, existing relationship limits);
- increase termination width / height retention and smooth the far continuation tangent policy;
- compute termination broad-radius evidence only on continuation interior outside inherited shoulder / rocker character bands;
- retain whole-zone non-degeneracy / cell-area evidence;
- treat far intentional-boundary transitions as explicit diagnostic / non-degeneracy evidence rather than ordinary character-band smoothness authority;
- keep Human Hero / Side / Top / Zebra + semantic multi-view review blocked until R3.2 Machine PASS.

PAP remains `BLOCKED`. Promotion remains `BLOCKED`. PR #91 remains Draft.
