# Modeling Worker v0.13｜G1 R1 Visual Decision

Decision: `VISUAL REVISE / PROJECT QA BLOCKED / CANDIDATE REVIEW BLOCKED`.

## Bound state

- Benchmark: `MW-V013-G1-ERGONOMIC-HANDHELD-SHELL`.
- Job State: G1 baseline construction + Machine runtime replay + `G1-R1-THUMB-SIDE-RELATION` executed.
- Design State: `REVISE`.
- Authority State: `WORKING_SOURCE`.
- R1 controlled upstream relation revision: PASS as an edit-isolation test.
- GitHub CI: not claimed; the v0.13 workflow write was blocked by the connector safety layer.

## Visual QA

R1 is rejected at Visual Gate for the following reasons:

1. the first reading remains too close to a generic leaf / lens / pillow volume rather than a sufficiently legible ergonomic handheld-shell relationship grammar;
2. `INTERFACE_DECK` does not read clearly enough as the second-priority controlled interface volume;
3. the thumb-side change increases fullness, but the relation change alone does not resolve the weak palm-volume / deck / termination hierarchy;
4. both longitudinal terminations remain overly point-convergent, weakening the non-automotive wrap reading;
5. the benchmark contract requires an interface opening / cavity condition, while the original Machine runner checked inset depth but did not explicitly bind that requirement.

## Root cause

`Relation / Surface Source`, not execution mesh.

Therefore local mesh correction is prohibited. The next legal action is an upstream relation/source revision that changes low-frequency Primary Curves and the interface boundary strategy while retaining sparse editable authority and derived execution topology.

## Validation correction

The original G1 Machine result is retained as evidence that its implemented checks passed, but it is not accepted as full benchmark Machine closure because `interface_opening_required` was not explicitly asserted. Full Machine closure must be replayed after the interface contract is made executable.

Boundary: no Human Project PASS, Candidate Authority, Canonical Authority, Release, engineering CAD, manufacturing or final product styling claim is established by this decision.
