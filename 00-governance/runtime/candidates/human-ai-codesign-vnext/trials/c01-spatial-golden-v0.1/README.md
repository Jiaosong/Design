# C01 Spatial Golden Trial v0.1

**Status:** `LOCAL_REVERSIBLE_TRIAL / NON_METRIC / NO_PROJECT_WRITEBACK / NO_PROMOTION`

## Real bounded question

How can the current H01 hand-lion topology become a spatially testable, reversible mechanism while keeping performer / tool / observer / service-reset territories distinct, keeping the resident continuity layer clear, and keeping `OFF_RETURN` complete?

## Current claim ceiling consumed from C01

- selected topology remains `OA` candidate;
- H01 host-fit is distributed/mobile and host-dependent;
- current use, permission, measured shell, measured route geometry, accessibility/fire and ADD-07 authority remain OPEN;
- no fixed H01 host, no measured dimensions and no committed wall/room geometry are introduced here.

## Alternatives

- `A_EDGE_DOCK.svg` — one support edge, outward borrowed field, clear resident passage.
- `B_SPLIT_SUPPORT.svg` — tool and service-reset split to opposite support edges; borrowed center stays free.
- `C_ZERO_STAGE_KIT.svg` — no dedicated stage/host object; mobile kit + temporary borrowed field + full return.
- `COMPARISON.svg` — same-condition comparison board.

These are **editable relation prototypes**, not survey drawings, not host selection, not architecture KEEP.

## Pre-steer reversible probe

The user instructed the system to continue the overall task without selecting, modifying, mixing, rejecting, reopening or explicitly deferring A/B/C. That instruction is recorded only as continuation intent. `human_steer` remains `PENDING`.

`PRE_STEER_SEQUENCE_PROBE.svg` preserves all three parent branches and changes the comparison from static topology to a shared `OFF → SETUP → ACTIVE → RESET → OFF` operating-sequence probe. It is autonomous reversible exploration before human steering, not a second-round delta based on human design feedback. The next consequential choice is narrowed to the actual burden each mechanism introduces: A concentrates return congestion, B depends on two eligible support interfaces, and C repeats setup/reset labor. No option is selected or promoted by this probe.
