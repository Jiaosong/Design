# OLEANDER Human–AI Co-Design vNext — Candidate Validation Status v0.1

**State:** `CANDIDATE / HUMAN_STOP_REQUIRED / NO CURRENT PROMOTION`
**Base:** `origin/main@6a52a5c5d9a94b71746466417404cf57e4e4b08f`

This status note reports what the isolated candidate has actually demonstrated. It is not a Current pointer, Project State, Design KEEP verdict, professional approval, field truth, plugin production-readiness certificate or Promotion decision.

## Structural status

- Three views remain projections of **one** OLEANDER architecture; no second Current/Project State/checkpoint/artifact registry/persistence authority is introduced.
- R-A through R-K are mapped `11/11` without deprecation in v0.1.
- The Control Card preservation review retains all `8/8` established baseline objects with identity preserved.
- `@OLEANDER 设计协作` 0.3.0 candidate is the active Human–AI Co-Design Session Kernel reference implementation; the installed/reference 0.2.1 package remains provenance only. The candidate consumes, rather than owns, Current Authority, Project State, Execution Receipt/checkpoint, native artifact identity, independent review, PAP and Promotion.
- Human action levels remain separate: `FEEDBACK_SIGNAL ≠ ITERATION_STEER ≠ DESIGN_DECISION ≠ DESIGN_KEEP ≠ PROMOTION_DECISION`.

## Session Kernel v0.2 implementation status

- Active candidate contract is now `OLEANDER_CODESIGN_SESSION_KERNEL_CONTRACT_v0.2.md`, backed by `OLEANDER_CODESIGN_SESSION_KERNEL_SPEC_v0.2.json` and the explicitly ephemeral `OLEANDER_CODESIGN_SESSION_CONTEXT_EPHEMERAL_v0.4.schema.json`.
- The main interaction correction is independent classification of `WORK INTENT × MUTATION DIRECTIVE × SUPPORT MODE × HUMAN ACTION LEVEL`, preventing generic continuation/read-only/support instructions from being silently promoted into design steering.
- `codesign_session_kernel_v0_2.py` implements a deterministic reference classifier plus option graph/branch transition, second-round, domain-adapter, mutation/Human-stop, defer-dependency, designer-support, progressive-context, file-placement and execution-surface routing guards. The generated `OLEANDER_CODESIGN_SESSION_KERNEL_VALIDATION_v0.2.json` currently reports 103/103 reference cases PASS. The added route cases prove Chat-native work stays in Chat, ready `BAIDU_STORAGE` work routes to inline COS, workstation-offline work falls back to the existing continuity execution-intent carrier, unavailable adapters HOLD, and unknown local capabilities fail closed without authority effect.
- `codesign_chat_cos_bridge_v0_1.py` has an actual same-session local/provider readback: the current Chat routed through COS to installed `oleander-baidu-storage@oleander-personal` v0.1.1 over stdio, `oleander_storage_probe` returned `connected` at `/OLEANDER_VAULT`, and `baidu_get_quota` returned `errno=0`. This proves the Chat-default local execution path at the storage-read claim ceiling only; it does not prove Project State mutation, Design KEEP, professional PASS or Promotion.
- A portable eight-file `plugin-v0.3.0-candidate/` package is frozen as `OLEANDER-Design-Plugin-0.3.0-CANDIDATE.zip`; package existence/hash does not prove successful installation or Current adoption.
- v0.2 adds explicit branch lineage, clause-scoped `human_actions[]`, typed/revisioned referent binding, computed Human-stop conditions, multi-Human decision-rights HOLD, domain adapter OPEN semantics and executable authority routing. `DESIGN_DECISION / DESIGN_KEEP / PROMOTION_DECISION` are classified but route only to existing OLEANDER authorities; they cannot mutate session branches or mint authority.
- Consequential steering now fails closed until owner-native typed/revisioned referents resolve. `DEFER` branch refs must belong to the current pending decision. Comparable options must carry `parent_refs / preserved_invariants / branch_status`, artifact refs must match bindings, and actual readback is bound to the exact artifact revision **and content hash**.
- A Human-steered second-round claim now requires explicit Human source + owner-native hash-bound decision-rights proof + owner-rule revision + typed/revisioned decision/referent binding + made artifact + same-revision/same-content actual readback + invariant-specific readback bindings whose readback explicitly enumerates the verified invariant. Schema-level `second_round_human_feedback_delta_verified=true` now conditionally requires the complete proof field set. An `ALLOWED_BY_EXISTING_OWNER_RULE` string, an unrelated readback ref, or a same-revision readback that did not verify the invariant is insufficient evidence.

## Real making / readback status

### Spatial — C01 H01

- Three editable non-metric relation mechanisms exist and have actual PNG readback.
- One observed first-round defect was repaired: C now shows service-reset as a distinct temporary territory.
- `PRE_STEER_SEQUENCE_PROBE.svg/.png` preserves A/B/C and exposes a shared `OFF → SETUP → ACTIVE → RESET → OFF` sequence.
- Its first render had a real text/card collision; the layout was repaired and reread successfully.
- The user's generic “continue and finish” instruction is classified only as continuation intent. `human_steer` remains `PENDING`; there is no second-round human-feedback delta yet.

### Digital / HCD — C04 Return / Service

- Three editable HTML/CSS/JS mechanisms exist: persistent return rail, context beacon, explicit return mode.
- Actual browser readback exists at stable 520×844 and one 844×390 landscape state.
- Structural audit now requires and passes a mechanism-specific marker for all A/B/C.
- True 390-CSS-pixel mobile emulation, representative-user usability and accessibility conformance remain open.
- Consequential human design steering remains pending.

### Physical product — C04 P02

- Three editable SVG attachment/service mechanisms exist with actual PNG readback.
- One observed B relation defect was repaired by making reversible clamp-return logic explicit.
- Structural audit now requires the no-independent-floor-leg invariant for every A/B/C/comparison surface; all pass.
- Structural adequacy, installation authority and field geometry remain open.
- Consequential human design steering remains pending.

## Regression status

The executable candidate suite currently reports `12` results: all `11/11` specified eval IDs plus one explicit `CD-05B` authority/source-drift extension. It has no hard failures and reports `PASS_WITH_PARTIALS`.

- `CD-03-NEGATIVE-STEER` is `PARTIAL`: the causal-hypothesis / no-durable-preference contract is present, but no real negative human-steer round has been executed.
- `CD-04-HUMAN-MIX` is `PARTIAL`: the C01 trial is correctly waiting for an actual option-referenced human MIX action; no second-round human-feedback delta is fabricated.
- `CD-08-DESIGNER-DEVELOPMENT` is `PARTIAL`: the distinction/transfer-question/no-profile contract is present, but no real designer-development interaction has yet been demonstrated.
- `CD-09-PLUGIN-REMOVAL-SURVIVAL` is `PARTIAL`: Project State, checkpoint and native authority carriers are proven to exist outside the plugin, but the installed plugin was not destructively uninstalled/reinstalled.
- `CD-10-CROSS-DOMAIN` is `PARTIAL`: all three domains have real editable artifacts and actual readback; Architecture resolves to its Current domain process, Digital/HCD resolves to its candidate domain process without promotion, and Physical Product truthfully remains `OPEN_NO_DOMAIN_NATIVE_PHYSICAL_PRODUCT_PROCESS_RESOLVED_DO_NOT_INVENT`. Consequential human A/B/C steering is also pending and is not inferred from generic continuation language.

Stale checkpoint, authority/source drift, presentation contamination, missing-native-surface behavior and multi-human decision-rights regressions have no hard failures at the candidate/fixture claim ceilings recorded in `regressions/v0.1/REGRESSION_RESULTS_v0.1.json`.

## Promotion-precondition ledger

| Precondition | Current candidate evidence state |
| --- | --- |
| `MIGRATION_MAP_NO_LOSS_REVIEW` | `PASS` at structural 11/11 R-layer + 8/8 baseline-object scope |
| `SPATIAL_REAL_CASE_TRIAL` | `OPEN` — real artifacts/readback done; human iteration steer pending |
| `DIGITAL_REAL_CASE_TRIAL` | `OPEN` — real artifacts/browser readback done; human iteration steer pending |
| `PHYSICAL_PRODUCT_REAL_CASE_TRIAL` | `OPEN` — real artifacts/readback done; human iteration steer pending |
| `HUMAN_STEERING_SECOND_ROUND_NATIVE_DELTA_VERIFIED` | `OPEN` |
| `NO_PARALLEL_STATE_OR_AUTHORITY_VERIFIED` | `PASS` structurally |
| `CURRENT_BOUNDARY_REGRESSION_PASS` | `PASS` for executed boundary cases; overall suite remains `PASS_WITH_PARTIALS` for CD-09/CD-10 |
| `INDEPENDENT_ARCHITECTURE_REVIEW` | `PASS_CANDIDATE_SKELETON` — first rereview found an action-level collapse; it was repaired and the fresh structural rereview passed |
| `EXPLICIT_HUMAN_PROMOTION_DECISION` | `NOT_REQUESTED` |

## Real human stop

The candidate has reached the point where further autonomous work would cross the steering boundary. C01 A/B/C remain unselected. A real `SELECT / MODIFY / MIX / REJECT / REOPEN / DEFER` action must reference the design object/options before a human-steered second-round native delta can truthfully exist.

No C01 Current writeback, no Notion Current change, no main-branch promotion and no architecture Promotion have occurred.
