# OLEANDER Presentation Replay — C04 Qingjiang — 2026-09-15

Status: **DRAFT P10 TARGET-CONDITION REPLAY / NOT CURRENT**.

Project: `PRJ-C04-QINGJIANG-SHISHU`.

Purpose: test the Presentation Layer / Style / Technique / truth-risk contracts against a real multi-medium project with real landscape imagery, project design imagery, model/technical support, AI reference, web, PDF/static export and motion.

No project presentation authority is changed by this replay.

---

## 1. Source-bounded replay facts

Latest retrieved presentation state includes:

- `QJ-D v1.1 = CURRENT PUBLIC VISUAL + 86s motion`;
- `FIG-C04-A00` and `FIG-C04-A01` = `PRIMARY_MAIN`;
- `C04-MDL-SEC-A` = `PRIMARY_SECTION_ALLOWED`;
- `C04-MDL-FIG-01` = `PROCESS_SUPPORT`;
- `C04-MDL-FIG-02` = `SUPPORT_ONLY / not R06 main`;
- project boundary remains `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`;
- real Qingjiang landscape is evidentiary/experiential anchor;
- model material is conceptual/process/support, not field-validated reality;
- current web identity is 52 pages and must not be confused with an older canonical 112-page specification;
- v1.16 uses 32 unique image sources / 32 unique pixel hashes; same source/crop/transparent derivative counts as one source and cannot occupy multiple semantic positions merely through derivative duplication;
- image hierarchy is `real Qingjiang / first-party scene > original user designs > object/product visuals > actual interface > technical diagrams > AI concept reference`;
- AI role images are reference-only and appear once;
- browser/static evidence is medium-specific: offline/static REV04 has 20 sections/20 refs/missing=0 and PDF full render, while live browser status remains `WAIT` in the retrieved evidence;
- public-first language is direct project language, not internal review/process jargon.

---

# 2. Source authority versus presentation dominance

The project makes a critical distinction:

`visual dominance must be earned by source/claim relevance`, not by rendering spectacle.

Real Qingjiang imagery may dominate experiential/public storytelling because it is the strongest available source for actual landscape condition.

Model views may dominate a technical relation page when the claim is specifically about modeled spatial/sectional structure, but model imagery cannot replace real landscape as field/site truth.

## Replay result

`CONFIRMED`.

### Replay rule

`P10/ROLE-RP01 PRESENTATION_DOMINANCE_MUST_MATCH_CLAIM_ROLE_AND_SOURCE_AUTHORITY; VISUAL_DRAMA_CANNOT_REASSIGN_SEMANTIC_ROLE`.

---

# 3. Semantic asset-position uniqueness

C04 v1.16 explicitly treats same source/crop/transparent derivative as one semantic source rather than allowing repeated variants to create false breadth.

This reveals a missing presentation-control concept:

`visual file instance ≠ semantic image source ≠ semantic position`.

A source can have several technical derivatives for responsive/export reasons, but those derivatives must not be counted as independent evidence/assets or occupy multiple primary semantic positions merely to inflate visual variety.

## Replay result

`UNDERMODELED → DELTA REQUIRED`.

### Replay rule

`P10/ASSET-RP01 DERIVATIVE_INSTANCES_SHARE_SEMANTIC_SOURCE_ID; DERIVATIVE_COUNT_CANNOT_INFLATE_ASSET_BREADTH_OR_INDEPENDENT_EVIDENCE`.

### Minimum presentation asset identity

```yaml
presentation_asset_id:
semantic_source_id:
source_authority_ref:
source_revision:
derivative_id:
derivative_operation:
semantic_role:
semantic_position:
truth_risk:
medium_variants: []
```

---

# 4. Primary / Support / Reference visual roles

The C04 hierarchy demonstrates at least four distinct visual roles:

- `PRIMARY_MAIN` — primary public/project claim carrier;
- `PRIMARY_TECHNICAL` / `PRIMARY_SECTION_ALLOWED` — primary for bounded technical/spatial claim;
- `SUPPORT / PROCESS_SUPPORT` — explains derivation/process/coordination;
- `REFERENCE_ONLY` — inspiration/speculation/context reference not evidentiary project proof.

These roles should not be inferred solely from size on page.

A support model may be physically large in a technical page while remaining SUPPORT at project-truth level.

## Replay result

`CONFIRMED_WITH_ROLE_MODEL_DELTA`.

### Replay rule

`P10/ROLE-RP02 VISUAL_ROLE_IS_SEMANTIC_AND_SCOPE_BOUND; DISPLAY_SIZE_ALONE_CANNOT_PROMOTE_SUPPORT_OR_REFERENCE_TO_PRIMARY_PROJECT_TRUTH`.

---

# 5. AI / synthetic truth-risk replay

AI concept imagery remains `REFERENCE_ONLY`.

Even if aesthetically coherent with D v1.1, it cannot establish:

- site condition;
- built condition;
- measured material condition;
- spatial performance;
- field user behavior;
- verified project geometry.

## Replay result

`CONFIRMED`.

### Replay rule

`P10/TRUTH-RP03 E3_REFERENCE_IMAGE_MAY_SUPPORT_CONCEPT_OR_MOOD_BUT_CANNOT_OCCUPY_EVIDENTIARY_SOURCE_ROLE`.

---

# 6. Web identity versus canonical specification

Current web identity is 52 pages while an older canonical specification had 112 pages.

This confirms:

`Presentation sequence/page count is a projection`, not canonical knowledge/project object count.

A public web experience may:

- omit internal pages;
- recombine source material;
- change sequence;
- use fewer screens;

without deleting or rewriting source authority.

## Replay result

`CONFIRMED`.

### Replay rule

`P10/SEQ-RP01 PRESENTATION_PAGE_SCREEN_COUNT_IS_PROJECTION_SCOPE_NOT_CANONICAL_SOURCE_COUNT`.

Reader/release metadata must preserve source mappings rather than infer missing content was deleted from source.

---

# 7. Medium-specific PASS replay

Static/offline evidence passes in the retrieved state, but live browser remains `WAIT`.

This is a strong real example that:

`PDF PASS ≠ STATIC WEB PASS ≠ LIVE BROWSER PASS ≠ MOBILE PASS ≠ VIDEO PASS`.

Each medium/read condition requires its own readback state.

## Replay result

`UNDERMODELED → DELTA REQUIRED`.

### Medium readback vector

```yaml
medium_readback:
  STATIC_EXPORT: PASS | REVISE | WAIT | FAIL | NOT_EVALUATED
  PDF_PRINT: ...
  DESKTOP_BROWSER: ...
  MOBILE_BROWSER: ...
  OFFLINE_BROWSER: ...
  VIDEO: ...
  EXHIBITION_PHYSICAL: ...
```

### Replay rule

`P10/MED-RP01 PRESENTATION_RELEASE_STATE_MUST_BE_MEDIUM_TARGET_CONDITION_SPECIFIC; PASS_ON_ONE_MEDIUM_CANNOT_AUTO_PROPAGATE_TO_ANOTHER`.

---

# 8. One public route / many evidence depths

The current presentation strategy protects one coherent public route while permitting deeper technical/support layers.

This confirms a useful separation:

`PUBLIC NARRATIVE ROUTE ≠ EVIDENCE DEPTH STRUCTURE`.

A presentation can have one main route without deleting support evidence. Technical depth may live in secondary pages, detail panels, interaction depth or review/debug routes.

## Replay result

`CONFIRMED`.

### Replay rule

`P10/DEPTH-RP01 ONE_MAIN_NARRATIVE_ROUTE_MAY_HAVE_MULTIPLE_EVIDENCE_DEPTHS; SIMPLIFICATION_MUST_NOT_DELETE_REQUIRED_PROOF`.

---

# 9. Style profile replay

C04 current presentation behavior most closely combines bounded aspects of:

- `S02 Editorial Narrative`;
- `S03 Civic / Institutional`;
- `S05 Hero-Led Portfolio` for selected strong landscape/project images;
- `S07 Exhibition Spatial` where physical/far-mid-near reading applies;

with evidence/support behavior from S01/S04 where technical pages require it.

Replay confirms that one project may use **profile composition by page/role** rather than one style profile across all media.

This should not become arbitrary style mixing.

### Missing concept

Add `STYLE_ROLE_ASSIGNMENT`:

```yaml
release_style_system:
  base_profile:
  role_overrides:
    HERO:
    TECHNICAL_PROOF:
    PROCESS_SUPPORT:
    DATA_ATLAS:
    MOBILE:
  invariant_axes: []
  allowed_variation_axes: []
```

## Replay result

`UNDERMODELED → DELTA REQUIRED`.

### Replay rule

`P10/STYLE-RP01 PROJECT_STYLE_CAN_USE_ROLE_SCOPED_PROFILE_OVERRIDES_ONLY_WHEN_INVARIANT_AXES_PRESERVE_ONE_COHERENT_SYSTEM`.

This is more precise than forcing one Style Profile or allowing uncontrolled mixture.

---

# 10. Field boundary persistence

The project keeps `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION` visible as truth boundary.

Presentation refinement, successful export or improved imagery must not erase this boundary.

## Replay result

`CONFIRMED`.

### Replay rule

`P10/BOUND-RP01 MATERIAL_FIELD_OR_PROMOTION_BOUNDARY_MUST_PERSIST_ACROSS_ALL_PUBLIC_DERIVATIVES_WHEN_RELEVANT_TO_INTERPRETATION`.

The exact visible wording may vary by audience, but semantic boundary cannot disappear.

---

# 11. Public language versus internal governance language

Current public direction avoids exposing internal audit language as primary visitor-facing narrative.

This reveals another useful distinction:

- governance/state metadata must remain available for truth/readback;
- public copy should use direct project language;
- internal terms such as KEEP/REVISE/Gate IDs need not dominate public narrative unless the audience is a review panel needing them.

## Replay result

`CONFIRMED_BOUNDED`.

### Replay rule

`P10/COPY-RP01 TRUTH_BOUNDARY_MUST_PERSIST_BUT_INTERNAL_GOVERNANCE_JARGON_NEED_NOT_BE_PRIMARY_PUBLIC_COPY`.

---

# 12. Replay-derived P10 data model refinements

Add to Presentation runtime:

### A. Semantic visual source identity

`semantic_source_id` separates source identity from exported instances.

### B. Visual semantic role

Controlled starter set:

`PRIMARY_MAIN | PRIMARY_TECHNICAL | SUPPORT | PROCESS_SUPPORT | REFERENCE_ONLY | NAVIGATION | DECORATIVE_BOUNDED`.

`DECORATIVE_BOUNDED` must not be used for evidence.

### C. Semantic position

One source may have technical derivatives, but primary semantic positions should be explicit and non-duplicative unless repeated use serves a documented continuity function.

### D. Medium readback vector

No scalar release PASS across heterogeneous media.

### E. Style-role assignment

One base style system may have page/role-specific overrides under invariant axes.

---

# 13. Replay-derived rules

- `P10/ROLE-RP01` dominance follows claim/source role, not visual drama;
- `P10/ASSET-RP01` derivative instances share semantic source identity;
- `P10/ROLE-RP02` display size cannot promote visual semantic role;
- `P10/TRUTH-RP03` E3 reference cannot become evidence source;
- `P10/SEQ-RP01` presentation page/screen count is projection scope;
- `P10/MED-RP01` medium-specific readback state required;
- `P10/DEPTH-RP01` one narrative route may retain multiple evidence depths;
- `P10/STYLE-RP01` role-scoped Style overrides require invariant system axes;
- `P10/BOUND-RP01` material truth boundary persists across derivatives;
- `P10/COPY-RP01` public language may simplify governance terminology without deleting truth boundary.

---

# 14. Replay disposition

`P10 TARGET-CONDITION REPLAY = FIRST_PASS_COMPLETE_WITH_DELTAS`.

The P10 replay confirms the style/technique system is directionally sound but needs five additional machine concepts before promotion review:

1. semantic visual source identity;
2. semantic visual role;
3. semantic position;
4. medium-specific readback vector;
5. role-scoped style profile assignment with invariant axes.

No Current promotion is authorized.