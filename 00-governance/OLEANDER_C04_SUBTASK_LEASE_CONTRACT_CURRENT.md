# OLEANDER C04 Subtask Lease Contract — Current

Status: `CURRENT / GOVERNANCE / C04-SCOPED`

Applies only to:
- `PROJECT_ID=PRJ-C04-QINGJIANG-SHISHU`
- `OBJECT_ID=PRJ-C04-DIGITAL-INTERACTION`

## Owner lock

`CURRENT_OWNER=PRESENTATION`

A bounded concurrent technical lease may exist as:

`VALIDATION_SUBTASK_LEASE=WEB_RUNTIME_DERIVATIVE`

This lease is not an owner transfer and does not change `CURRENT_OWNER` or lifecycle `NEXT_OWNER`.

## Legal lease scope

The lease is legal only when all conditions remain true:

1. The main Work Object remains owned by `PRESENTATION`.
2. The lease solves only the confirmed `/恩施` MASTER Web Runtime blocker.
3. `VALIDATION` does not alter design relations, public narrative, MAIN/SUPPORT verdicts, final visual judgment, or Presentation hierarchy.
4. Lease outputs are restricted to source/materialization, LOD or geometry simplification, texture derivative preparation, GLB/glTF or equivalent browser carrier, lazy-load/runtime mechanics, and browser/runtime evidence.
5. Every returned artifact, metric, limitation, and HOLD is attached to the same `OBJECT_ID` and returned to `PRESENTATION`.
6. The next `PRESENTATION` execution on this object must preferentially consume the returned runtime derivative before opening unrelated production work.

Within this scoped lease, formal `SENT → ACCEPTED` owner handoff is not a precondition for VALIDATION to execute the technical subtask. Formal handoff remains required for an actual lifecycle owner transfer.

## Required governance state

Represent the object as:

`CURRENT_OWNER=PRESENTATION / VALIDATION_SUBTASK_LEASE=ACTIVE|RETURNED|HOLD`

Do not rewrite `CURRENT_OWNER` or `NEXT_OWNER` merely because the lease is active.

## Violation classes

- `SUBTASK_LEASE_SCOPE_VIOLATION`: VALIDATION crosses into design judgment, public narrative, MAIN/SUPPORT verdict, or final visual production.
- `OWNER_WAIT_LOOP`: PRESENTATION repeatedly emits handoff/wait state because the runtime derivative is absent, without producing the executable design/interaction container delta that remains within its own scope.
- `LEASE_NOT_CONSUMED`: a legal active lease exists but VALIDATION diverts to unrelated Training Mode instead of executing the bounded runtime derivative.

## Current activation

Current producer frontier: `GitHub PR #465 / agent/c04-web-v1-12-currentize-20260830`.

Current `/恩施` MASTER runtime request is documented on that frontier in `05-cases/c04-qingjiang-stone-book/web-v1.12-game-line/C04_PRODUCT_INTERACTIVE_RUNTIME_HANDOFF_CURRENT.md`. Its requested derivative outputs fall inside this lease when treated strictly as technical runtime preparation and evidence. Presentation retains final camera, crop, hierarchy, motion restraint and KEEP/REVISE authority.

Current lease state at governance activation:

`CURRENT_OWNER=PRESENTATION / VALIDATION_SUBTASK_LEASE=ACTIVE`

Truth boundary:

`SUBTASK LEASE ≠ OWNER TRANSFER ≠ DESIGN KEEP ≠ FIELD PASS`
