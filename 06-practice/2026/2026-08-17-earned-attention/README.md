# 2026-08-17｜Web UI｜Earned Attention

Status: `CANDIDATE / POST-READBACK PASS FOR PRACTICE`

## Learning object
Linear 2026 UI refresh, using Linear's official design/process and changelog pages as the professional reference.

## Candidate rule
`TASK CONTENT > CURRENT DECISION / RETURN > SUPPORTING CONTENT > NAVIGATION CHROME > UTILITIES`

In dense interfaces, secondary chrome should remain available and legible without competing with the user's current task. Component consistency does not imply equal visual weight.

## OLEANDER transfer
Synthetic C04 hidden-assistant mockup. Same content in Before/After; only contrast, border, fill, type weight and active-state hierarchy change. `LANDSCAPE FIRST` remains the largest field and `RETURN` becomes the current decision layer. No C04 live-service, direction, distance, site image, or field validation is claimed.

## Files
- `before_equal_chrome.svg`
- `after_earned_attention.svg`
- `before_mobile_static.svg`
- `after_mobile_static.svg`

## Review boundary
Desktop and mobile static pixel readback completed locally through SVG/CairoSVG. Chromium binary was detected, but headless screenshot timed out in the current execution surface, so Browser Runtime Review remains `HOLD`.

`Artifact existence != Design quality` and `STATIC EXPORT EVIDENCE != BROWSER PASS` remain in force.
