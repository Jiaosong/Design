# OLEANDER Artifact Review v1.1 — CH10-P01 Resource Choreography v0.3

## Object
`C04 / CH10-P01 / resource choreography v0.3`

Status: `EXECUTED / ACTUAL PIXELS REOPENED / PAGE-SCOPED GITHUB REVIEW REQUIRED / NO SELF-KEEP`.

## Root-cause repair from v0.2
v0.2 still read as a collage: landscape base + right relation strip + inserted physical diagram. v0.3 changes the carrier itself:

- `MOVE / CHOOSE` → a **full-scene cable / cross-river resource** owns the viewport;
- `LOOK / OBSERVE / PASS` → a **photo-only Qingjiang landscape crop** owns the viewport;
- `OBSERVE` → optional reading appears only as a bounded line/fold + one short prompt; no card;
- `RECOVER` → the same landscape is dimmed and only the existing body/recovery drawing is extracted as a transparent overlay;
- no synthetic relation line remains; the cable image itself carries relation;
- public producer/process copy (`RESOURCE OWNERSHIP`, current-owner labels, Gate explanations) is removed.

## Authority
- CH08 v0.4: WHOLE / RELATION / BODY; MOVE / LOOK / OBSERVE / RECOVER / CHOOSE / PASS; Dynamic Ownership.
- CH10 v0.4: optional reading, reveal, withdraw, Return interruption.
- CH14 Current: palette / typography / LINE-PAGE-TRACE only.

## Resource transformations
All transformations are non-generative image operations: crop, resize, contrast adjustment, background removal.
No new scene pixels were generated.

## Runtime readback
- 1920×1080 and 390×844 actual Chromium pixels reopened.
- horizontal overflow = 0.
- tested JS/page errors = 0.
- minimum interactive control height = 81 px.
- keyboard MOVE → LOOK → OBSERVE = executed.
- Reduced Motion running animations = 0.
- state visibility contract: OBSERVE reading=1; all other states reading=0; RECOVER body overlay=.94, all other body states=0.

## Producer-side visual attack — not an independent verdict
Resolved compared with v0.2:
1. no right-hand photo strip / collage seam;
2. no baked upstream headline inside the landscape crop;
3. no technical-page paragraph or governance copy in RECOVER overlay;
4. no synthetic cable relation trace over a real cable resource;
5. mobile OBSERVE reads as scene → fold → prompt, not overlay stack;
6. public page no longer explains internal resource-ownership method.

Open for independent review:
1. whether the Qingjiang landscape crop and cable crop are sufficiently project-specific at first read;
2. whether RECOVER technical linework is visually integrated enough or still too diagram-like;
3. integrated-Web image allocation / duplicate-pixel scan across other chapters;
4. whether the six-state bottom rail is appropriately subordinate on desktop and mobile.

## Gates
- Artifact / runtime evidence: `EXECUTED`.
- AR-S09 durable independent binary retrieval: `OPEN` until external retrieval closes it.
- GitHub page-scoped review: `REQUIRED` for this exact v0.3 revision.
- Independent Professional Design Gate: `PENDING`.
- Producer issues no `KEEP / MAIN / Professional Design PASS`.
