# C04 CH10-P03 v0.1｜Artifact Review

Status: `EXECUTED / ACTUAL PIXELS REOPENED / PRODUCER QC COMPLETE / INDEPENDENT PROFESSIONAL DESIGN REVIEW PENDING / NO SELF-KEEP`.

## Authority / scope

- Object: `C04 / CH10-P03` — Companion Pool Editor.
- Authoring authority remains `CH10 v0.4 = Core 8 + Companion Pool + Trip Subset`.
- Page obligation: make `MERGE / SUBSTITUTE / OFF` visible as real editorial behavior, not as an internal governance table.
- P01 and P02 remain separate review targets; this page does not supersede or promote either.

## Executed design behavior

- `MERGE`: R03 / R08 / R10 are not shown as three permanent content cards; their source identities are absorbed into one `天书组页`.
- `SUBSTITUTE`: the weaker/repetitive Tian-shu family recedes to provenance while `许愿台／板栗树` becomes the single active behavior folio.
- `OFF`: optional content fully withdraws and explicitly leaves `ROUTE / RETURN` continuing.
- `R04` and `R11` remain edge candidates, not equal permanent pages.
- `巨帆启航` is not promoted into CH10 to fill a nominal count.

## Source / truth boundary

- No image generation. No raster content images. Public pixels are HTML/CSS/SVG/live text.
- The public guide supports a known wishing behavior around a chestnut tree (`许愿树` / 板栗树). The current label `许愿台／板栗树` comes from CH10 v0.4; P03 does **not** independently prove a constructed platform (`台`).
- `天书组页` is an editorial semantic merge only; P03 does not claim exact site imagery, coordinates, geometry or a field-observed object view.
- Safety / route pressure remains higher priority than R11 content.

## Actual Chromium execution evidence

- 1920×1080 MERGE / SUBSTITUTE / OFF: `scrollWidth = clientWidth = 1920`; state control min height `111px`; one `aria-pressed=true`.
- 390×844 MERGE / SUBSTITUTE / OFF: `scrollWidth = clientWidth = 390`; state control min height `131px`; one `aria-pressed=true`.
- Keyboard: focus `MERGE` + ArrowRight → `SUBSTITUTE`.
- Reduced Motion: panel/button transitions = `0s`.
- In-page error collector = empty. Fresh CDP target exception/error events = none.

`Runtime PASS ≠ Design PASS`.

## Gate state

- `INDEPENDENT PROFESSIONAL DESIGN VERDICT = PENDING`.
- `BEST-EXISTING C04 WEB / CH14 VISUAL BENCHMARK = REQUIRED`.
- `WHOLE-PROJECT RESOURCE / RHYTHM DUPLICATION CHECK = OPEN BEFORE PROMOTION`.
- Frozen Drive package persistence is closed separately by `DURABLE_BINARY_RECEIPT_v0_1.md`; persistence does not issue a design verdict.

No producer `KEEP / MAIN / PROFESSIONAL FINISH PASS` is issued.

Truth boundary: `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT GPS / NOT FOR CONSTRUCTION`.
