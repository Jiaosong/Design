# Independent Design Review Request — CH10 P02 v0.3.1

Review exact 1920×1080 and 390×844 pixels, not producer description. Return only `KEEP / REVISE / REJECT / HOLD` with severity-ordered defects, root-cause layer and Best-Existing benchmark comparison.

Check:
1. landscape / attention hierarchy;
2. whether the bottom Core-8 spine reads as task/dashboard UI rather than optional reading;
3. whether one shared field communicates attention grammar without pretending eight site-specific views;
4. typography, spacing, composition and mobile hierarchy;
5. R13 `BODY / ROUTE / RETURN > READING` priority;
6. R07 dual naming `仓禀峰／仓廪峰`;
7. consistency with Current C04 / CH14 visual grammar without giving Brand route/state authority.

Do not treat runtime, hash, GitHub, Drive or source compliance as Design PASS. Producer verdict remains `PENDING / NO SELF-KEEP`.