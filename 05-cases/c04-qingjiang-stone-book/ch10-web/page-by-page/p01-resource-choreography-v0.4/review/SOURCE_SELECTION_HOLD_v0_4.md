# CH10 P01 v0.4 — Unique Landscape Source Selection HOLD

## Authority chain
`C04 Current → CH10 v0.4 → Landscape Media Rights Register → Official Web Media Shortlist → Official Web Media Technical Audit → Current Image Consumption Ledger`.

## Result
Do **not** bind a real-photo content image to P01 v0.4 in this iteration.

### Why
1. Official/operator website assets are `RIGHTS PASS / PROJECT-USE APPROVED`, but they still require `NODE + TECH + image-ledger` clearance.
2. Current technical audit records the four materialized official candidates as:
   - R05 `OW-20230616-2a923422a` = 1080×525 / `FAIL_LT2400`;
   - R05 `OW-20230711-mnq9o6767b` = 1080×608 / `FAIL_LT2400`;
   - R06 `OW-20230718-v0ii0wjlhe` = 1200×800 / `FAIL_LT2400` / Science Hero HOLD;
   - R01 `OW-20230619-pbxolmbgx1` = 1080×605 / `FAIL_LT2400` / responsive art-direction required.
3. R05 assets are specifically valuable node candidates; consuming them as a generic P01 chapter carrier would also spend their one semantic-image slot before R05 page allocation is resolved.
4. Known C22 macro sources are not free: current register records one `REJECTED_NOT_ELIGIBLE / DO_NOT_REUSE` and one `RESERVED / C22-CONCEPT-MASTERPLAN`.
5. The Image Consumption Gate forbids using a reserved/consumed asset or pretending crop/grade/mask creates a new identity.

## Current decision
- P01 v0.4 remains **raster content-image count = 0** and explicitly `SOURCE-SAFE STUDY`.
- Unique real Qingjiang landscape = `HOLD / NEED NEW AVAILABLE LEDGER-CLEARED SOURCE`.
- This HOLD does not block source-safe interaction/runtime production, but it **does block landscape-first finished-pixel readiness and any MAIN/KEEP claim**.
- Do not restart generic public-index search without new material evidence (higher-resolution official/original asset, newly materialized unused official frame, or explicit project-ledger release).

No producer Design verdict is issued.