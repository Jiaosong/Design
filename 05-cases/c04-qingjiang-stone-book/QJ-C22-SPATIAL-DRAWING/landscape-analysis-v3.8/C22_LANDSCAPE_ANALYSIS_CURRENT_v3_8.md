# C04 / C22 Landscape Analysis — CURRENT v3.8

Status: `EXECUTED / SOURCE-CARRIER-BOUND / SELF-CHECKED / INDEPENDENT REVIEW PENDING`

Project: `PRJ-C04-QINGJIANG-SHISHU`  
Drawing object: `DRW-C04-C22-01`  
Truth boundary: `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`

## 1. Current decision

Landscape Analysis v3.8 adopts **source-carrier precedence**.

For a relation already legible in the first-party guide, the current analysis does **not** redraw a cleaner substitute.

Current chain:

`FIRST-PARTY SOURCE VISUAL → SPATIAL READING → EDITORIAL ANCHOR / SEPARATE ANALYTICAL TRANSLATION`

not:

`SOURCE → CLEANER CUSTOM LINE / NODE / FIELD → PRESENT AS SOURCE-BOUND ANALYSIS`.

This currentizes the same principle already present in the earlier `C22_OFFICIAL_GUIDE_MINIMAL_v1` provenance object: `DIRECT PRIMARY VISUAL / NO REDRAW`, with explicit non-actions `no terrain redraw / no route redraw / no node repositioning / no AI-generated base map / no metric promotion`.

## 2. Current source carriers

### `SRC-A01 / 红花峰林景区手绘导览`

Role: `DIRECT PRIMARY SPATIAL CARRIER`.

Use directly for:
- Qingjiang / two-bank relation;
- north-bank service/arrival field;
- cross-river cable relation;
- south-bank multi-branch / loop walking topology;
- macro attachment of named landscape/facility points to that network.

Do not redraw these as a new river polygon, custom cable alignment, custom route centerline or repositioned node system merely for presentation.

### `SRC-A02 / 红花峰林景区全景导览`

Role: `RELATION-LEVEL CROSS-CHECK`.

Use to cross-check multi-branch / loop character and macro facility/landscape relations. It does not upgrade the hand-drawn guide into survey geometry.

## 3. What is allowed to be translated separately

### R2 journey order

`SCHEMATIZE / SEQUENCE_BOUND` only.

It may be drawn as a separate journey sequence because the first-party guide does not establish the current research order. It must not be overlaid on the guide in a way that inherits geographic authority.

### R06

No plan view cone / viewer field.

Current analytical carrier is existing section/GA support where body-ground-view relation can be discussed without inventing plan geometry.

### R13

No plan threshold/compression footprint.

Current carrier is section/sequence: `APPROACH → COMPRESSION → RELEASE / EXIT → RETURN`, with exact width, length, safety edge and field state OPEN.

### Return

No independent map-like Return curve.

Return is expressed as service / journey closure and state logic unless a source later closes a distinct physical alignment.

## 4. Current object separation

- **Route v3.8** = official-guide composition / topology transcription object.
- **Landscape Analysis v3.8** = source-carrier precedence + analytical translation control object.

They are related but not interchangeable.

Route transcription may create an editable derived carrier for the Route object when its source/translation boundary is explicit. Landscape Analysis does not inherit that derivative as higher geometry authority than the first-party guide.

## 5. Superseded / provenance behavior

Do not propagate as current landscape-analysis truth:
- v3.4/v3.5 custom landscape-analysis route geometry;
- broad colored route envelopes;
- map-like Return curves;
- R06 plan view cones;
- R13 plan threshold footprints;
- R2 direction arrows drawn over pseudo-map route geometry.

v3.6 is translation-repair provenance.  
v3.7 is direct-source correction provenance.  
v3.8 is the current source-carrier control state.

## 6. Required register

Machine-readable current control:

`C22_SOURCE_CARRIER_PRECEDENCE_v3_8.json`

The register follows candidate Technical Drawing Skill PR #172:

`SPATIAL_TRANSLATION_PROTOCOL + SOURCE_CARRIER_PRECEDENCE`.

The Skill is still Draft/unpromoted, so this case use is a calibrated candidate application, not a claim that the Skill itself is promoted.

## 7. Review boundary

This currentization closes only the producer-side source/translation discipline.

It does **not** grant:
- `PIXEL KEEP`;
- `MAIN KEEP`;
- `PROFESSIONAL FINISH PASS`;
- survey / GIS validity;
- field approval;
- engineering approval;
- merge or Promotion.
