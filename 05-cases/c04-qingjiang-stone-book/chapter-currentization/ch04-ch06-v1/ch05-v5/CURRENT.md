# CH05 Current Producer Pointer

Current producer visual candidate: `v8.2.1 REFINED`.

Content authority remains CH05 Current / v0.3.

Current producer page logic:
- P01 = `Journey Atlas / ROUTE-03` — full `ROUTE-03 = USER LOCKED CURRENT` remains read-only spatial authority; 17 anchors are retained in a compact name rail; Journey/Priority is a separate relational layer with no stage-to-location registration.
- P02 = `Body on Real Terrain / GLO-30` — CH02 Current GLO-30 DSM (`193×94`, approx. 30 m, `EPSG:32649`) is the terrain carrier. Six A–F sample transects are shown on the real terrain and map directly to six profile excerpts; body states remain design inference, not observed exact locations.
- P03 = `Attention Relay / Current Objects` — fixed qualitative lanes across `R01 / ROUTE-03 / R06 / R13 / RETURN`; primary-ownership arrows, transition gates and Return-urgency override clarify handoff; no percentage claim.
- P04 = `STOP ≠ READ / Decision Grammar` — MOVE/PASS bypass STOP. LOOK/OBSERVE/RECOVER/CHOOSE converge through one bracket into the STOP Gate. Optional explanation is eligible only when OBSERVE + attention are available; behavior adds no ROUTE-03 node.

Supersession:
- v3 = SUPERSEDED / PROVENANCE ONLY.
- v4 = REJECT / SUPERSEDED / INVALID IMAGE BINDING / PROVENANCE ONLY.
- v5 = SUPERSEDED / PROVENANCE ONLY.
- v6.1 = REVISE / SUPERSEDED VISUAL TARGET / PROVENANCE ONLY.
- v7 = INTERMEDIATE PRODUCER REBUILD / PROVENANCE.
- v7.1 = SUPERSEDED PRODUCER VISUAL TARGET / PROVENANCE.
- v8 = INTERMEDIATE REFINEMENT / PROVENANCE.
- v8.1 = SUPERSEDED PRODUCER VISUAL TARGET / PROVENANCE.
- v8.2 = INTERMEDIATE FINE-REFINEMENT / PROVENANCE.
- v8.2.1 = CURRENT PRODUCER CANDIDATE.

Durable package:
- `C04_CH05_REFINED_v8_2_1_PACKAGE.zip`
- bytes `9,038,517`
- SHA256 `3aa90966a7c5599bd4aeb2ac13f8d651be3ce88cee0be73b6edb9300c6dcb9a8`
- Drive ID `1GKv2m7jruMA5B211B7aXaAfHf0YbpVlY`
- raw Drive re-fetch = `9,038,517 bytes`
- ZIP integrity after re-fetch = `30 entries / testzip=None`
- re-fetched SHA256 = identical.

Review state: `PRODUCER CANDIDATE / ACTUAL TARGET-SIZE READBACK COMPLETE / CURRENT-SOURCE-BOUND / DRIVE PERSISTED + RAW REFETCH VERIFIED / INDEPENDENT PROFESSIONAL DESIGN REVIEW PENDING / NO SELF-KEEP / NO_PROMOTION`.
