# C04 CH05 v6.1 — Qingjiang-specific same-source rebuild

Argument:
`REAL QINGJIANG JOURNEY → SPATIAL CONDITION → BODY STATE → ATTENTION BUDGET → BEHAVIOR / VALID DESIGN RESPONSE`.

Material changes after v6 actual-pixel readback:
- P01 keeps the Qingjiang Experience Transect; the `ROUTE-03` inset exposes named source anchors for stronger project recognition without stage registration.
- P02 removes the six-card wall. One continuous relational Qingjiang body section carries `WATER → CABLE → TRANSFER → WALK → OBSERVE → PASS → RETURN`.
- P03 uses the exact same P02 base condition spine and only reveals Attention ownership + `FULL / LIGHT / OFF`; same-source gate is explicit.
- P04 removes the table. Three-domain grammar = `Current Condition → 12-action vocabulary → Valid Design Response`, with `STOP ≠ READ / TASK` as the hard center condition.
- Desktop P01 now exposes Return as an explicit journey stage.
- 390 semantic recompositions retain the Qingjiang condition names, Attention columns and Valid Design Response layer; no-loss mobile compression.

Image / source discipline:
- raster content image = `0`;
- image generation = `NOT USED`;
- `ROUTE-03 = USER LOCKED CURRENT v3.4`, consumed read-only;
- no stage-to-location registration;
- no new route geometry;
- no R06/F01/legacy image reuse.

Truth boundary:
`FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT GPS / NOT FOR CONSTRUCTION`.

State:
`PRODUCER CANDIDATE / ACTUAL TARGET-SIZE READBACK COMPLETE / DRIVE PERSISTED + RAW REFETCH VERIFIED / INDEPENDENT PROFESSIONAL DESIGN REVIEW PENDING / NO SELF-KEEP / NO_PROMOTION`.