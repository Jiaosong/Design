# CH05 v6.1 Actual Preview Readback

Review type: `PRODUCER ACTUAL-PIXEL READBACK` — **not** independent Professional Design review.

Targets reopened:
- 4 × `1920×1080` final PNG surfaces;
- desktop four-page contact sheet;
- grayscale contact sheet;
- 4 × semantic `390×844` mobile surfaces + mobile contact sheet.

## OLEANDER Professional Design Crit dimensions

### 1. Claim / first-read
v5 root cause: after title deletion, P02–P04 remained generic methodology diagrams.

v6.1 repair: all four pages now share one visible project subject: `Qingjiang Journey → Spatial Condition → Body → Attention → Behavior / Response`.

### 2. Professional completeness
P02 was rebuilt from six isolated body cards into one continuous relational body section. P03 uses the same base section rather than a separate chart family. P04 now preserves condition, full 12-action vocabulary and valid response in one grammar.

This proves a materially deeper authoring candidate; it does **not** prove Professional Design PASS.

### 3. Template / AI residue
No AI image generation. Equal-card walls in v6 P02/P03 and the table-like P04 were treated as producer readback blockers and removed before v6.1. Remaining visual language follows CH14 `CONTEMPORARY EDITORIAL + LANDSCAPE SPACE / LINE / PAGE / TRACE` rather than dashboard/card chrome.

### 4. Evidence → Design impact
- P01: Qingjiang modality sequence establishes why priority changes; `ROUTE-03` remains read-only spatial authority.
- P02: spatial conditions visibly change body state and design priority.
- P03: identical P02 base geometry demonstrates that Attention ownership changes while the journey object stays the same.
- P04: behavior is translated into valid design response without creating a new route or task sequence.

### 5. Deletion test
- Removing `ROUTE-03` named-anchor inset from P01 weakens project recognition and spatial authority, so it remains.
- Removing P02 shared condition spine breaks the body argument and P03 same-source reveal, so it remains.
- Removing P04 `Valid Design Response` collapses the chapter into behavior description, so the mobile version was repaired to retain it.

### 6. Rhythm
Four-page sequence now reads as one argument rather than four unrelated diagram types:
`Journey → Body → Attention → Behavior/Response`.

### 7. Cross-media consistency
Desktop and 390 versions preserve the same condition identities and claims. P02→P03 uses the same source object `QINGJIANG_CONDITION_SPINE_v6_1`; P03 only adds Attention reveal.

### 8. Portfolio value
Producer readback shows material improvement in Qingjiang recognizability and chapter coherence relative to v5, but portfolio `KEEP / MAIN KEEP` remains unavailable until independent Professional Design Gate review.

## Producer verdict
`ACTUAL TARGET-SIZE READBACK COMPLETE / MATERIAL ROOT CAUSES REPAIRED / INDEPENDENT PROFESSIONAL DESIGN REVIEW PENDING / NO SELF-KEEP / NO_PROMOTION`.

Truth boundary:
`FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NTS / NOT GPS / NOT FOR CONSTRUCTION`.