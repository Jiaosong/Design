# CH08-P01 v1.1｜Producer Design Crit

Role: `PRODUCER-SIDE FINISHED-PIXEL CRIT / NOT INDEPENDENT PROFESSIONAL DESIGN VERDICT`.

## Authority consumed
- CH08 Current v0.4 / PR #294;
- `CH08_MASTER-DESIGN-STRATEGY_v0_4.md`;
- existing editable `CH08_MASTER_EXPERIENCE_ATLAS_v0_4.svg`;
- CH14 Current color / typography / information grammar;
- OLEANDER Existing Mature Design First;
- OLEANDER Story & Board;
- OLEANDER Delivery QC;
- Artifact Review system.

## First-read target
The page must read in this order:

`WHOLE → RELATION → BODY → CURRENT ATLAS → NORMAL / OVERRIDE`.

It must **not** read as:
- six equal subsystem cards;
- a second route map;
- a fixed whole-journey priority stack;
- a governance dashboard;
- a brand manual page;
- a new App system.

## R0 defect found in actual 1920×1080 pixel readback
The initial desktop composition used:
- hero columns `.72fr / 1.28fr`;
- H1 max `96px`.

Result: `最后进入身体。` wrapped with the final `体。` isolated onto an extra line. This weakened the first-read cadence and made the left block look accidental rather than editorially controlled.

### Repair executed
- hero columns: `.80fr / 1.20fr`;
- left minimum width increased;
- H1 desktop cap: `88px`;
- right three-scale field retained sufficient visual authority.

### R1 pixel result
At `1920×1080`, the headline now holds as three deliberate semantic lines:
1. `先获得整体，`
2. `再跨越关系，`
3. `最后进入身体。`

No horizontal overflow and no broken bound vector were recorded.

## Composition finding
- First visual gate: the page now has one dominant proposition and one dominant vector strategy field rather than a card wall.
- Existing-design gate: the current v0.4 Atlas is embedded directly and is not redrawn or cosmetically replaced.
- Hierarchy: the long-form Atlas is downstream of the three-scale thesis rather than competing with the title.
- CH14 use: palette/type grammar is consumed at medium strength; CH08 retains its own SYSTEM composition.
- Technical/governance material is subordinate and confined to locks, captions and truth boundary.

## Mobile finding
At `390×844`:
- page width = viewport width;
- no document-level horizontal overflow;
- H1 remains a controlled three-line first read;
- the native three-scale summary recomposes vertically;
- the full 2400×1350 master atlas remains a source plate in a deliberate horizontal-scroll viewport, rather than being illegibly shrunk into 390px;
- NORMAL / OVERRIDE recomposes vertically.

## Producer gate
Producer-side blocking pixel defect after R1: `0 recorded`.

This statement is **not**:
- `KEEP`;
- `MAIN KEEP`;
- independent Professional Design PASS;
- final integrated Web PAGE approval;
- field / operator / safety / engineering validation.

Next valid gate: `INDEPENDENT PROFESSIONAL DESIGN REVIEW` on the immutable source target after PR/CI readback.

Truth boundary: `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`.
