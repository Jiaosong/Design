# C04｜云水倚｜简约产品作品页 v0.1

## 作品定位

这是一个独立的产品作品展示版本，不覆盖现有 `/qingjiang/`，也不替代 C04 Web PR #465。

公开阅读只围绕产品：

`产品出现 → 为什么需要 → 身体如何使用 → 产品如何变化 → 关键部件 → 清江中的位置 → 收束`

公开页面不展示 OLEANDER 内部治理、验证、状态或工程流程语言。

## Current source binding

- Product object: `云水倚 / C04_YUNSHUIYI_REBUILD_MASTER_v003`
- Current project authority: `C04_PROJECT_CONTROL_CARD_CURRENT.md`
- Machine-return evidence: GitHub Actions artifact `9965907493`
- Source-defining visible relations retained in the presentation derivative:
  - bilateral side rails
  - five timber back slats
  - fold-down padded seat
  - paired hinge/pivot relation
  - diagonal support arms
  - railing-edge attachment relation

The Web SVG is a presentation derivative for clear product reading. It does not replace the native Blender master or claim exact geometric/source-fidelity closure.

## OLEANDER design route used

Existing-first inputs materially used before production:

- `oleander-design-process`
- `PRODUCT_FORM_AFFORDANCE_SERVICEABILITY_EXTENSION.md`
- Notion `PRAC-20260816-05｜Dominant Field & First-read`
- Notion `PRAC-20260817-12｜Page-role Visual Rhythm`

Applied design decisions:

- product owns first visual read;
- use action precedes form explanation;
- interaction explains product state rather than decorating the page;
- page roles vary in visual mass and density instead of repeating one card template;
- Qingjiang remains scene context and never replaces the product as the work being presented.

## Native output

`index.html`

Self-contained HTML/CSS/JS + editable inline SVG. No external runtime image dependency.

## Interaction

- scroll progress / current section indicator;
- Hero pointer response;
- `收起 / 倚靠 / 坐下` state switch;
- four product-part hotspots;
- desktop/mobile responsive layout;
- reduced-motion fallback.

## Readback

Local exact-source Chromium readback completed on the same HTML content later written to GitHub:

- 1920×1080: no horizontal overflow;
- 390×844: no horizontal overflow;
- `坐下` state interaction: triggered correctly;
- `软座面` part interaction: triggered correctly;
- desktop/mobile first-read screenshots inspected.

Remote GitHub readback after write confirms `index.html` contains the complete artifact.

## Current design boundary

This v0.1 is a product-presentation work, not an engineering or field approval artifact. The current native 云水倚 model still has an independent source-fidelity review open in the C04 Physical line. This Web derivative therefore does not claim final model fidelity, manufacturing readiness, site-fit approval or field verification.

## Next design move

Do not add more explanatory copy. The next meaningful visual improvement is the product view itself: increase three-dimensional/form distinctiveness and body-contact readability while preserving the same simple interaction grammar.
