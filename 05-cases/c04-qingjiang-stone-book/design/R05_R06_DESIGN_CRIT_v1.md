# C04｜R05 + R06 Scene Deepening｜Design Crit v1

Project: `PRJ-C04-QINGJIANG-SHISHU`  
State: `SINGLE-PROJECT DEEPENING / TOTAL ARCHITECTURE NOT FROZEN`  
Truth: `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION`

## R05｜红花石林

### CURRENT STATE
`SCENE / CONCEPT KEEP / INTERACTION KEEP / PIXELS REVISE`

### ACTUAL MATERIAL DELTA
- Real editable HTML interaction prototype.
- Editable SVG `SEE → DO → FEEDBACK → END/EXIT` diagram.
- Four audience depths share one scene.
- Optional selection feedback; no score, unlock, timer, route gate, or 13/13.
- Explicit digital retreat and always-available exit.

### STRONGEST OUTPUT
Interaction closure: the scene has a clear optional beginning and a real end. The visitor can skip it entirely; after one observation the interface returns attention to the landscape.

### WEAKEST OUTPUT
Landscape first-read is still an abstract peak silhouette. It proves interaction structure but is not Qingjiang-specific enough for final MAIN pixels.

### ONE REVISION
Replace the abstract first-read with a source-grounded Qingjiang / Honghua peak-forest visual or a tightly controlled derived crop/diagram. Preserve the interaction overlay as vector/editable UI. Do **not** invent a precise viewpoint, peak geometry or weather state.

### VERDICT
`KEEP_AFTER_REVISION / MAIN CANDIDATE, NOT MAIN PIXEL YET`

## R06｜多级阶地·不对称河谷

### CURRENT STATE
`SCENE SYSTEM / CONCEPT KEEP / BODY-USE KEEP / DIGITAL LOGIC KEEP / PIXELS + PROOF REVISE`

### ACTUAL MATERIAL DELTA
- Editable HTML scene system.
- Editable body-use / parallel-proof SVG.
- Landscape First → Recover → Optional Reveal → Continue/Return logic.
- Audience-depth switch.
- Digital state OFF / LIGHT / short Reveal.
- Body-use contract for short leaning/recovery support.
- Parallel proof interface for `SEC-A + R06 node`.

### STRONGEST OUTPUT
Body, digital retreat and proof layers now serve one visitor action instead of appearing as separate report chapters. Older/low-stamina first read is correctly rest/return-first.

### WEAKEST OUTPUT
Current first-read valley is still schematic in the persisted branch. The proof area is an interface slot, not the actual authoritative SEC-A / R06 node source. This cannot be called final MAIN.

### ONE REVISION
For the final pixel version, use the existing source-grounded R06 landscape as the dominant first read, then place the actual current SEC-A and R06 node beside/behind it as a parallel proof drawer. Model must remain subordinate to landscape. Do not redraw or fabricate missing construction details.

### VERDICT
`KEEP_AFTER_REVISION / MAIN CANDIDATE, PROOF SOURCE REPLACEMENT REQUIRED`

## Cross-system checks
- Q3 Play: R05 materially improved.
- Q4 Rest: R06 now has an explicit recovery rhythm rather than a facility-only explanation.
- Q5 Digital Presence: both scenes explicitly show appearance/retreat instead of only stating a principle.
- Q8 Body Use: R06 now follows `BODY STATE → APPROACH → ACTION → SUPPORT → DURATION → EXIT`.
- P0 identity: R05/R06 remain Thirteen-Imprint content identities and do not own route authority.
- No AI image used in these persisted source files.
- All formal text/UI/diagrams remain HTML/SVG editable/vector.

## Promotion boundary
These files are valid single-project design sources and may merge as `KEEP_AFTER_REVISION`. Merge does **not** mean final Web order, Field PASS, Design FINAL, or MAIN pixel promotion.
