# C04 CH01 Web v1.1 — Project-wide Unique Image Gate

Standalone CH01 Web successor. Generated whole-page images are **not** embedded. Typography, diagrams, icons and system graphics are native HTML/CSS/inline SVG.

Raster content-image use: exactly one semantic slot in CH01 (`CH01-P01-MAIN`), bound to `OW-20230616-2a923422a`. P02–P05 use no raster content imagery.

The included project-wide ledger is a partial current audit because the complete integrated C04 Web surface register is still `NOT_YET_REGISTERED`. No project-wide uniqueness PASS is claimed yet.

The recoverable local/user bundle includes the actual P01 presentation derivative and finished-pixel readback. GitHub records the editable Web source, manifest, image-use ledger and production receipt; binary project-level persistence remains separately tracked and does not imply Design PASS.
