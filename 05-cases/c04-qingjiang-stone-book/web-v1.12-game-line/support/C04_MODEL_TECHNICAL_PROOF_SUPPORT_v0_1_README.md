# C04 MODEL TECHNICAL PROOF SUPPORT v0.1

Status: `CANDIDATE / VALIDATION-SUPPORT / NOT PUBLIC RUNTIME OWNER / NO_PROMOTION`.

This bounded support module re-applies the 2026-08-27 OLEANDER 3D×Web role-binding training to the Current C04 public-Web producer PR #353 without changing the 18-section runtime structure.

## Current mapping

Current public source already contains an AI+3D process card for `04 / 3D` and states that 3D checks form, body posture, path occupation, sightline and component relations. Real Qingjiang landscape remains the project first visual and source-grounded experience carrier.

The support module therefore declares:

- `MODEL_ROLE = TECHNICAL_PROOF`;
- `PRIMARY_PROJECT_EVIDENCE = REAL_QINGJIANG / CURRENT SOURCE-GROUNDED LANDSCAPE`;
- current poster locator = `../assets/fluid_rest_object.png`;
- exact model bytes/version/hash = `UNRESOLVED IN THIS TRAINING RUNTIME`;
- public truth boundary = `研究级模型 / NTS / Field & Engineering Open`;
- runtime integration = `HOLD` until Current model source and full public-Web asset integrity are resolved.

## Fail-closed behavior

If `fluid_rest_object.png` cannot load, the module shows `MODEL SOURCE PENDING` and keeps the technical-proof role/truth/evidence chain visible. It does **not** draw or generate a substitute model.

Local actual Chromium support readback against the intentionally unavailable asset:

- desktop 1440: `scrollWidth=1440`; state = `SOURCE UNBOUND / FALLBACK ACTIVE / DO NOT SUBSTITUTE`; screenshot SHA256 `def1493c4158a44394b52164e6e46de423df31cacd34828ddaa501d6cac48ea6`;
- mobile 390: `scrollWidth=390`; state = `SOURCE UNBOUND / FALLBACK ACTIVE / DO NOT SUBSTITUTE`; screenshot SHA256 `847031ae7bfd29f32b720f9415f17ade6fd0859765f361f9ba5262dbb3d2a3e7`.

These screenshots are local readback evidence only; GitHub byte persistence of the PNGs is not claimed.

## Boundary

This file is a support candidate inside the existing Production PR #353. It is not linked into `index.html`, is not the public runtime owner, does not recover missing model bytes, and does not alter the current C04 hero, route, Field state, engineering state, or Design promotion.

`FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`.