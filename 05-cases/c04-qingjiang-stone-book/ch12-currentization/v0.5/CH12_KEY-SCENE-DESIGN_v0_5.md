# CH12｜关键场景设计｜v0.5 Project-Unique Individual Pages

State: `AUTHORING SUCCESSOR / INDIVIDUAL PAGE PRODUCTION / PROJECT-WIDE IMAGE-UNIQUENESS COMPLIANT IN CURRENT AUDITED SCOPE / NO_PROMOTION`

## Hard correction from v0.4
The project-wide Web image uniqueness gate supersedes v0.4's repeated-image authoring pattern. One underlying content-image source may occupy only one semantic image slot across the complete C04 Web. Crop / resize / grade / mask / overlay / breakpoint variants do not create a new source identity.

Therefore v0.5 uses **zero raster content-image sources across A01–A09**. Each page is a standalone editable SVG and uses a different evidence/representation medium. This is an authoring correction, not a claim that final scene photography is unnecessary. Real-scene content-image slots are reserved for later project-wide source allocation.

## 9 standalone pages
- A01 Selected Scene Atlas — journey-field composition; no raster.
- A02 R01 Cable Moving View — moving-view spatial schematic; no raster; authentic moving stimulus remains HOLD.
- A03 R05 Discovery — LOOK / FRAME / COMPARE; explicit content-image slot HOLD; CH01 source is not reused.
- A04 R06 Landscape First — landscape/body field; `R06 FROZEN`; real R06 image slot reserved and not duplicated.
- A05 R06 Same-source Reveal — single semantic component model; final Web must implement reveal as state/layer, not second static slot.
- A06 R06 Body × Audience — body/recovery/user-depth composition; P02 relation only, not location claim.
- A07 Route Decision — Continue / Return decision grammar only; not ROUTE-03 geometry; location open.
- A08 R13 Enter + Pass — compression/aperture composition; `BODY / ROUTE ON`, `READ / PLAY OFF`.
- A09 R13 Exit + Re-recognition — reopening/look-back/Return composition; Memory handoff begins after exit.

## Chapter contract
`REAL SCENE FIRST → BODY / ROUTE DUTY → OPTIONAL OBSERVATION / READING → MINIMUM SUFFICIENT DIGITAL / PHYSICAL → WITHDRAW / RETURN`

Final public scene imagery must still honor `REAL QINGJIANG FIRST`; v0.5 only prevents premature duplicate-source allocation while the full project Web surface/image register remains incomplete.

`ROUTE-03 = LOCKED CURRENT / R06 = FINISHED + FROZEN / FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`
