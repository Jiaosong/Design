# CH12｜关键场景设计｜v0.3 compact snapshot

> **CURRENT POINTER｜2026-08-19**  
> 本文件保留为 `chapter-content-sync/v0.3` 的 compact chapter snapshot。CH12 最新 authoring frontier 已深化为：  
> `05-cases/c04-qingjiang-stone-book/ch12-currentization/v0.4/CH12_KEY-SCENE-DESIGN_v0_4.md`  
> 唯一当前入口：`ch12-currentization/CURRENT.md/.json`。  
> v0.4 不删除本文件；它将原 7 个场景摘要深化为 **Selected Scene Proof Chapter**，并完成 9 个 1920×1080 authoring surfaces 的实际像素回读。

Role: 用少量关键场景证明 CH08/CH09/CH10/CH11/CH13 的设计关系如何在真实游程中发生，而不是建立节点目录。

Current scene contract:

`REAL SCENE FIRST → BODY / ROUTE DUTY → OPTIONAL OBSERVATION / READING → MINIMUM SUFFICIENT DIGITAL / PHYSICAL → WITHDRAW / RETURN`

## Current selected scenes

### R01 / Cable Moving View
`BEFORE ORIENT → DURING OBSERVE → AFTER RECOGNIZE`。移动视域与通行优先；不创建强制 UI 或中途装置。真实连续 moving stimulus 仍 HOLD。

### R05 / Discovery
`LANDSCAPE → LIGHT PROMPT → LOOK / FRAME / COMPARE → OPTIONAL REVEAL → CONTINUE`。互动 KEEP；authoritative Honghua landscape pixel 未关闭时保持 MAIN PIXEL HOLD，不用 generic Qingjiang / AI 替代。

### R06 / Landscape + Reveal + Body
`R06 = FINISHED / FROZEN / NO REOPEN`。只继续使用 Landscape First、same-source Relation Reveal、optional deep read、body recovery、Return。P02 只可作为 body-recovery support relation，不证明其已落位 R06；Fluid Rest / Qingfengyin 继续在 CH13 / CH18 竞争。

### Route Decision / Continue vs Return
CH12 唯一新增的场景类型，不是新景点。用于压力测试 CH09 `Route / Return Physical Touchpoint Family`：ROUTE-03、Continue/Return、Paper、Human、Digital OFF、CLOSED/UNKNOWN fail-closed 与 Brand semantic firewall。Exact location = OPEN，不伪造坐标。

### R13 / Enter → Pass → Exit → Re-recognition
PASS 中 `BODY / ROUTE ON`，`READ / PLAY OFF`。越收束，Digital / Brand 越退场；退出后 Route / Return 重新取得主权，Memory 才开始累积。Current visual = remote currentized design / not site photo；width / safety / accessibility / exact view 继续 OPEN。

## Current chapter decision

CH12 的核心空间语法由一对动作构成：

`R06 REVEAL → R13 WITHDRAW`

即：**打开关系，再把关系交还给景观。**

R02 / R07 / R09 / R12 只有在内容与场景证据足够时才单独深化，不为补页数平均展开。

`FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT FOR CONSTRUCTION`
