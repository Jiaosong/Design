# CH12｜关键场景设计｜v0.3 深化

Role: 通过具体场景证明 CH08/CH09/CH10/CH11/CH13 的关系真的成立。

## 1｜R01 / Cable
继续作为“移动视点保护”场景：`BEFORE ORIENT → DURING OBSERVE → AFTER RECOGNIZE`。新增内容只来自 CH09 Cable Choreography，不创建 R01 强制 UI 或装置。

## 2｜R06｜冻结
`R06 Experience = FINISHED / FROZEN / NO REOPEN`。

CH12 只继续使用：
- Landscape First；
- Same-source Relation Reveal；
- optional deep read；
- body recovery；
- Return；
不重新设计空间或新增假平台。

## 3｜R13｜继续作为退场证明
`APPROACH → PREPARE → ENTER → PASS → EXIT → REOPEN → LOOK BACK / RETURN`。

PASS 中 `BODY / ROUTE ON`，`READ / PLAY OFF`。任何新内容不得在压缩段争夺身体注意力。

## 4｜本轮新增验证场景类型：ROUTE DECISION SCENE

不是新景点，而是给 CH09 `Route / Return Physical Touchpoint Family` 一个真实测试模板。

场景必须同时检查：
- 一个真实分支/决策问题；
- 当前 Route-03 关系；
- Continue vs Return；
- Paper 对应；
- Human help；
- Digital 是否可退场；
- CLOSED / UNKNOWN 时是否 fail-closed；
- Brand 是否压过功能信息。

在 exact location 未验证前保持 `LOCATION OPEN / FIELD OPEN`，不得伪造节点坐标。

## 5｜后续场景深化
R02 / R07 / R09 / R12 仅在内容和场景证据足够时单独深化，不为补页数平均扩展。