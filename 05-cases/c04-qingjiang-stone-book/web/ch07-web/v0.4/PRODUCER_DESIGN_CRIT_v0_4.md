# C04 CH07 Web v0.4 — Producer Design Crit

State: `PRODUCER REBUILD COMPLETE / NO SELF-KEEP / INDEPENDENT DESIGN REVIEW PENDING`.

## Trigger consumed
The previous v0.3 correctly repaired page identity but still allowed method/route graphics to compete with Qingjiang. The current visual frontier requires a frontstage order of:

`REAL QINGJIANG / PROJECT SCENE → RELATION TRACE → METHOD / QUALIFICATION`.

The C22 v4.6 `70 / 20 / 10` ratio is consumed as a composition calibration for image-led landscape surfaces, **not** as a mechanical percentage rule for every CH07 surface.

## Material visual delta
- `CH07-P01-S01`: real Qingjiang river/cable landscape now owns first read; ROUTE-03 becomes a subordinate trace. The source image crop removes embedded project title/footer without changing landscape relation; route geometry is exact-source paths with labels removed and presentation styling only.
- `CH07-P02-S01`: R13 spatial compression owns the surface; attention logic moves to a narrow supporting spine.
- `CH07-P03-S01`: Same-source Reveal remains the strongest evidence-led page; paired states now use a 66/34 hierarchy so the base landscape clearly dominates the reveal.
- `CH07-P04-S01`: framework/card feel is removed. `0 / NO DESIGN` becomes one editorial field; intervention levels remain secondary.
- `CH07-P04-S02`: large red object-card is removed. Attack Test becomes a restrained editorial field with negative space and a subordinate outcome rail.

## Producer findings after actual-pixel reopen
- P01 source-embedded legacy text was visible in R0. Crop was repaired from `(420,60,1920,1000)` to `(610,70,1920,930)` and re-rendered.
- No horizontal overflow at 1920 or 390.
- All five content-image instances decode. R06 appears twice only inside one `CH07-P03-S01` Same-source semantic component.
- P04 remains intentionally more abstract because it follows three evidence/project-scene surfaces; it is not allowed to precede them in the chapter sequence.

## Open design gates
- Independent Professional Design Verdict on exact v0.4 pixels.
- Whole-project Web image/resource allocation: P01 hero source must be checked against the integrated Web ledger before Promotion.
- Live-navigation Browser PASS remains unclaimed because the runtime uses exact-source `page.set_content` with inlined local assets.

No producer `PIXEL KEEP / MAIN KEEP / PROFESSIONAL FINISH PASS` is issued.
