# C04 App｜ROUTE-03 Currentization Module v0.1

Executable bounded delta for `PRJ-C04-DIGITAL-INTERACTION`.

It does **not** replace the full App or claim v1.28. It tests one current-authority correction: the App route world must consume the exact locked `ROUTE-03` carrier rather than maintain the stale embedded v1.20–v1.27 guide-route curve.

## Version lineage now consumed

This module is **not** based on v1.27 in isolation. The corrected inheritance model is:

`v1.25 specialist baseline → v1.26 Product/Journey/IA/Service architecture → v1.27 navigation/game architecture → ROUTE-03 currentization + current exploration grammar`.

From **v1.26**, preserve APP_INIT; QUICK / DEEP / FAMILY with `MODE != PERSONA`; page architecture; Return Guard architecture; Digital OFF/no-phone continuity; UNKNOWN/CLOSED fail-closed behavior; Reduced Motion; and Framework → specialist Skill governance order.

From **v1.27**, preserve the 4-primary IA `TODAY / ROUTE / MY BOOK / SERVICE`; READ/R06/R13 as route-context descendants; route-child rail; return-to-route behavior; and the inherited v1.26 framework layer.

Do **not** preserve the v1.25-inherited/custom guide-route geometry as current route authority. `ROUTE-03 = LOCKED CURRENT` supersedes that geometry only; v1.26 remains a required architecture ancestor.

The v1.26 and v1.27 portable files both retain a stale internal HTML title reading `清江石书 App v1.24｜Visual System`; treat this as metadata drift, not as version identity.

See `VERSION_LINEAGE_RECONCILIATION_v0_2.json` for the explicit preserve / supersede boundary.

Key behavior: `INTENT → SCOUT → COMMIT → REVEAL → RETREAT / RETURN`.

Truth boundary: `FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT GPS / STATUS UNKNOWN`.

Review status: producer/runtime QA only until an independent finished-pixel Design Crit is performed.
