# C04 App Source Consumption Readback v0.2

## Current authority
- `PRJ-C04-DIGITAL-INTERACTION [P3]` is the current App workstream identity.
- `ROUTE-03 = LOCKED CURRENT` is route geometry/source authority.
- App route presentation uses a viewBox-only derivative; no independent current route topology is redrawn.

## Version lineage consumed
- v1.25: specialist visual/contextual baseline only.
- v1.26: Product / Journey / IA / Service architecture ancestor. Source readback confirms framework state storage, `APP_INIT → MODE(optional) → OFFLINE_PREP`, fail-closed before startJourney, reading-mode metadata, Return Guard priority, and framework APIs.
- v1.27: navigation/game architecture successor. Preserve 4-primary IA and route-context child hierarchy as the full-App integration target.
- ROUTE-03 currentization supersedes stale route geometry authority only; it does not erase v1.26 framework architecture.

## Currentization-specific corrections
- v1.26 OFFLINE PREP mentioned A/B/C/D + 18 GUIDE nodes because its then-current route topology came from v1.25. v0.2 does **not** repeat that stale topology claim; it binds the route check to ROUTE-03.
- Full R01–R13 reading pages are not duplicated into this bounded module. Their identities/architecture remain an integration requirement for writable v1.27.
- v1.26/v1.27 internal `<title>` still says v1.24; treated as metadata drift only.
- legacy `C04-WS-*` pointers are provenance, not current workstream identity.
