# C04 App v1.30 — Producer Implementation Readback

Status: `EXECUTED / RUNTIME READBACK COMPLETE / INDEPENDENT FINISHED-PIXEL REVIEW REQUIRED / NO_PROMOTION`

## Material delta actually implemented
- Removed the v1.29 runtime-built pseudo Logo. The mobile header accepts only the Web CH14 Stone Seal asset carrier; missing binary fails invisible rather than substituting a fabricated mark.
- Rebuilt exploration state naming to the canonical OLEANDER game-UI grammar: `SEEK → APPROACH → FOCUS → ENTER → REVEAL → WITHDRAW → RETURN`.
- Repaired the prior v1.29 mismatch where CSS listened for `SEEK / APPROACH` while JS emitted `INTENT / SCOUT`; the first two game-response stages therefore could not execute consistently.
- ROUTE / R06 / R13 are world-first fields. Optional reading enters from local context and withdraws back to the world.
- ROUTE-03 source carrier is preserved; no second route geometry was added.
- R13 keeps `PLAY OFF / BODY FIRST` and high-priority Return.
- Ink/paper assets remain material-only, non-evidence, replaceable layers.

## Runtime evidence
See `RUNTIME_READBACK_v1_30.json`.

- 390×844: no recorded page/JS errors; horizontal overflow 0; minimum visible button 44×44; keyboard ROUTE pan +40 px; Return attack closes lower-priority context and lands on SERVICE/RETURN.
- 430×932: same tested properties; minimum visible button 44×44; keyboard ROUTE pan +40 px.
- 390×844 Reduced Motion: 0 running animations; state equivalence retained and ENTER/context state remains operable.

## Open implementation gates
- `LOGO_ASSET_BINDING = OPEN`: exact Web CH14 `QJ_STONE_SEAL_MARK_v2_1.svg` bytes are not materialized in the current sandbox. The wrong mark is removed; no substitute is drawn.
- Cross-reload persistence still requires an allowed non-opaque http(s) origin if it is to be re-proven in this execution environment.

## Review boundary
This record reports implementation and runtime facts only. It does not issue `KEEP`, `MAIN`, a numeric design score, or Professional Design PASS.

`FIELD OBSERVED=0 / FIELD MEASURED=0 / G1F HOLD / NO_PROMOTION / NTS / NOT GPS / STATUS UNKNOWN`
