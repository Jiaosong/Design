# oleander-web-ui｜Project Usage Feedback Current

Status: `OWNER-LOCAL PROJECT LEARNING / CANDIDATE SUPPORT / SKILL USAGE PROVENANCE NOT ESTABLISHED / NO PROMOTION`

Purpose: preserve real C04 browser-integration failures and bounded transfer rules inside the existing `oleander-web-ui` Candidate route without inventing historical Skill usage or self-promoting it to an installed execution owner.

## Provenance rule｜2026-09-07

The C04 Web evidence below proves a real browser failure, readback, and bounded repair path. It does **not** by itself prove that an exact `oleander-web-ui` version was read/selected before the failing project action.

Therefore the current classification is:

`PROJECT_LEARNING_EVIDENCE / SKILL_FEEDBACK_ORPHAN`

If direct pre-execution/in-action evidence for exact Skill/version use is later recovered, a real `SKILL_USAGE_TRACE` may be restored. Otherwise this remains project learning attached to the existing Candidate owner, not historical `PROJECT_USAGE_EVIDENCE`.

## Current evidence｜2026-09-07

### C04 Web PR #465｜semantic source carrier failure

Real browser readback reached desktop/mobile target sizes and proved several runtime/layout behaviors while still failing because two claim-bearing image carriers depended on unstable Google Drive thumbnail delivery.

Observed separation:
- page structure / responsive layout / interaction state could execute;
- the browser could still fail the project because required semantic/source assets did not load;
- changing one remote URL for another remote URL does not solve delivery authority or browser stability by itself.

Bounded learning:

`SOURCE AUTHORITY ≠ DELIVERY CARRIER`

For claim-bearing source assets:
1. keep the source/master identity and provenance unchanged;
2. materialize or otherwise obtain controlled bytes when project authority allows;
3. preserve hash / derivative identity / crop role;
4. bind a browser-stable repo/deployment-controlled derivative;
5. test the actual target viewports/states in the real browser;
6. if the carrier fails, classify `ASSET DELIVERY FAIL` locally rather than calling the whole Web design invalid.

Preferred sequence:

`SOURCE AUTHORITY → EXACT BYTES / DERIVATIVE IDENTITY → CONTROLLED DELIVERY CARRIER → HTML/CSS/JS BINDING → REAL BROWSER → VIEWPORT / STATE READBACK`

Do not use:

`REMOTE THUMBNAIL FAIL → SWAP TO ANOTHER OPAQUE REMOTE URL → ASSUME FIXED`.

## Anti-pollution project learning

The same PR exposed that adding files with `CURRENT/current` in their names without a valid Current identity creates governance failure even when the page itself renders.

Bounded learning:
- decide `Native Master / Current Derivative / Support / Evidence / Provenance / Temporary` role before using `CURRENT/current` naming;
- browser integration must not manufacture parallel Current identities merely because a file is locally convenient;
- Anti-Pollution PASS is separate from browser/render PASS and separate from Design KEEP.

## Failure / counterexample

Looks like PASS but is not:
- all layout tests pass while a required claim-bearing source image is 404/blocked;
- the source exists in Drive but browser delivery is unstable;
- a screenshot shows the page correctly once, but the production browser cannot reproducibly load the same source carrier;
- a repo file is named `*_current.*` but has no legitimate Current authority role;
- browser PASS is used to claim final Design KEEP.

## Future usage evidence

For a future real project action to count as `PROJECT_USAGE_EVIDENCE` for `oleander-web-ui`, preserve direct pre-execution/in-action Skill provenance and then record:

`PROJECT_ID / OBJECT_ID → SKILL_REF + VERSION/COMMIT → RULE/CAPABILITY_USED → NATIVE/EDITABLE ARTIFACT → ACTUAL BROWSER READBACK → RESULT → REPAIR/RETEST/HOLD → DOWNSTREAM EFFECT → TRANSFER BOUNDARY → FEEDBACK_ACTION`

Do not create or distort a project to produce promotion evidence. TRAINING_MODE Practice may add controlled evidence but does not count as a second real-project usage.

## Boundary

This evidence supports browser delivery-carrier discipline and Current-role hygiene only. It does not prove:
- historical use of an exact `oleander-web-ui` version;
- the source itself is authoritative merely because bytes were materialized;
- final visual quality;
- backend/service correctness;
- accessibility completeness;
- promotion of `oleander-web-ui` from Candidate to installed owner.

Maturity: `PROJECT LEARNING / REAL FAILURE + READBACK / REPAIR PATH DEFINED / SKILL USAGE PROVENANCE NOT ESTABLISHED / FINAL C04 BROWSER CLOSURE NOT YET COMPLETE / NO PROMOTION`.
