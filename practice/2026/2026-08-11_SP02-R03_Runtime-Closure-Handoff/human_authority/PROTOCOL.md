# P03｜Human Authority GUI Protocol

Human Authority 是正式 Provider，不是降级路线。

在任意已安装且正常授权 Rhino 8 + Grasshopper 的真实电脑：打开最终 `.gh/.ghx` → 完整 solve → 确认五个 sink 与五个 Parameter Viewer → 运行 runtime collector → 补 Grasshopper canvas / adverse-case screenshot → 写 provider receipt → provider-neutral validator → 独立 Artifact Review。

禁止：只有口头确认；没有 native definition 的截图；用离线 SVG/PNG 冒充 Parameter Viewer；手改 JSON 使 validator 通过。
